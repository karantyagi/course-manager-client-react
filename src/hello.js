import React from 'react';

class HelloWorld
    extends React.Component {
    render() {      // render tells us what we want to display as component
        return(
        <h1>Hello, World component !</h1>
        )
    }
}

export default HelloWorld;